# 金钱剪刀 V1 启动入口

开始任何任务前，必须完整读取并遵守：
`/Users/dang/Desktop/金钱剪刀/V1/CLAUDE.md`

## 硬边界

- 唯一 Git 主线是 `main`；过去的 `day1`—`day19` 与功能分支只存在于已验证恢复包中。
- 正式服务器只允许按任务明确授权操作；没有独立部署授权时，禁止写入、部署或重启。
- 项目源码根目录固定为 `/Users/dang/Desktop/金钱剪刀/V1`。
